
# ЛР №2 — Кластерный анализ в распознавании образов

БГУИР, ВМСиС, 2025

Проект для лабораторной работы по курсу ЦОСиИ (часть 2). Реализация кластерного анализа изображений на OpenCV.

Добавлена поддержка VTK для отображения серого изображения в 3D.

Исполняемый файл: `with_opencv`

Набор тестовых изображений лежит в каталоге `images/`.

## Условие лабораторной работы

Лабораторная работа №2

«Кластерный анализ в распознавании образов»

1. Исходные данные: проект ЛР №1 (при необходимости — внести правки).
2. Ознакомиться с теорией по методичке (стр. 26).
3. Разделить фигуры на самостоятельные объекты (рекомендуется использовать математическую морфологию — эрозия, 3D представление и др.).
4. Вычислить характеристики объектов (например, площадь, периметр, центр масс и др.).
5. Разделить объекты на 3 класса на основе алгоритма кластеризации (например, k-means/k-medians/k-medoids) и вычисленных признаков.
6. Раскрасить объекты в соответствии с классом (класс 1 — красный, класс 2 — синий, класс 3 — зеленый).

## Реализация в проекте

Конвейер кластерного анализа объектов на изображении (OpenCV):

- Загрузка изображения
- Морфологическая обработка (эрозия, дилатация)
- Выделение объектов
- Вычисление признаков объектов (площадь, периметр, удлинненость)
- Кластеризация объектов (например, k-means)
- Раскраска объектов по классам
- Отображение промежуточных результатов
- Визуализация серого изображения в 3D с помощью VTK

Общие утилиты:

- `include/image.h`, `src/image.cpp` — класс-обёртка для изображения и функция `ShowImages` для компоновки нескольких изображений в одном окне

## Зависимости

- CMake 3.18+
- C++17 совместимый компилятор
- OpenCV (core, imgcodecs, imgproc, highgui)
- VTK (для 3D визуализации)
- NVIDIA CUDA Toolkit (для сборки `with_cuda`)
- Опционально: Qt5 Widgets (для выбора файла через диалог и 3D отображения; можно отключить)

Пример пакетов для Ubuntu/Debian (версии могут отличаться):

```bash
sudo apt update
sudo apt install cmake build-essential libopencv-dev
# Для Qt-файлового диалога (опционально)
sudo apt install qtbase5-dev
# CUDA Toolkit устанавливается из официальных репозиториев NVIDIA
```

Примечания по CUDA:

- Требуется видеокарта NVIDIA и установленный драйвер + CUDA Toolkit
- В CMake выставлена архитектура: `native`

## Сборка

Опции CMake:

- `-DENABLE_QT=ON|OFF` — использовать Qt6 для диалога выбора файла (по умолчанию ON)
- `-DCMAKE_CUDA_ARCHITECTURES=native|<arch>` — архитектура CUDA (по умолчанию native)
- `-DCMAKE_BUILD_TYPE=Release|Debug` — тип сборки (по умолчанию Release)

Сборка:

```bash
mkdir -p build
cd build
cmake -DENABLE_QT=ON ..
cmake --build . -j
```

В результате будет собран исполняемый файл:

- `build/with_opencv` — реализация на OpenCV (CPU)

## Запуск

Проект поддерживает два способа выбора входного изображения:

1) С Qt (ENABLE_QT=ON) — при запуске откроется диалог выбора файла. Аргументы командной строки не требуются.
2) Без Qt (ENABLE_QT=OFF) — путь к изображению обязателен аргументом.

Примеры:

```bash
# Qt включён (откроется диалог)
./with_opencv

# Qt отключён (нужно передать путь)
./with_opencv ../images/1.jpg
```

Управление:

- Отображение идёт в нескольких окнах (мозаика шагов обработки)
- Закрытие: нажмите клавишу ESC в активном окне для выхода

## Пример работы

![Пример кластеризации](./images/example/Clusters_screenshot_07.10.2025.png)

## Структура проекта

```text
include/
 image.h                 # Базовый класс Image<T>, ShowImages
src/
 image.cpp               # Реализация ShowImages
 mains/
  main.cpp              # Конвейер на OpenCV (CPU)
images/                   # Примеры входных изображений
CMakeLists.txt            # Конфигурация сборки (цель: with_opencv)
```

## Лицензия

Учебный проект. Используйте по назначению в рамках лабораторных работ.
